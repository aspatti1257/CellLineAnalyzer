
class RSENConfig(object):

    def __init__(self, binary_cat_matrix, p_val, k_val, combine_gene_lists):
        self.binary_cat_matrix = binary_cat_matrix
        self.p_val = p_val
        self.k_val = k_val
        self.combine_gene_lists = combine_gene_lists
